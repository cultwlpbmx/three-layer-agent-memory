# 脱敏案例：长任务线中三层记忆如何防止上下文漂移

> 本案例来自范式的真实出处项目，已脱敏：不包含任何服务器 IP、域名、路径、凭据、用户数据。
> 只保留结构性与方法论信息。

## 项目背景

- **领域**：家庭教育垂直领域的 AI agent（家庭关系成长，非学科知识）
- **栈**：Flutter（客户端）+ FastAPI（后端）+ MongoDB + 云端 LLM
- **周期**：数月，数十个版本（V3.x → V5.x → V6），单版本常含十余项反馈修复
- **工作模式**：人 + AI agent 协作；AI agent 负责实现、测试、部署、迭代

## 不用本范式时会出什么问题

在引入三层记忆之前，典型痛点：

1. **初心漂移**：长任务线里上下文被压缩，agent 开始忘掉"我们不做学科知识"这条宗旨，给出的方案偏向教知识。
2. **重复踩坑**：上一版本踩过的坑（如某 API 在否定语境下误触发）下一版本又踩，因为没有持久化的"真因"记录。
3. **返工**：加功能时没先问"是否服务家庭关系成长"，做完才发现偏离主线，推倒重来。
4. **不可接力**：换一个 session / 换一个 agent 接手，要花大量 token 重新理解"为什么现在是这个架构"。

## 引入三层记忆后的变化

### 表层锚定初心

`表层/00-项目总览.md` 把核心宗旨、哲学、应用准则逐字保留。无论上下文怎么压缩，recall 第 1 步就读这一篇。效果：

- "是否服务家庭关系成长"这条准则在新功能评审时**每次都在场**，偏离主线的功能当场被砍。
- 危机干预协议（自杀/自伤/家暴/虐童 → 热线优先）永远在表层，不会被新版本覆盖。

### 中层留住真因

每阶段一篇任务记录，"遇到的困难"节强制写**真因**而非表象。效果：

- "某 API 在否定语境下误触发"被记成真因（"关键词匹配未做否定语境排除"），下一版本同类功能直接绕开。
- INDEX 时间线让"上一次做到哪、卡在哪"一眼可见，接力成本骤降。

### 深层驱动进化

`深层/AI深度思考.md` 按 `## YYYY-MM-DD` 追加。真实演化片段（脱敏）：

- 某日深层写下："当前对话姿态偏向'给方法'，违背'倾听优先'准则——这是结构性偏差，不是个例。"
- 该判断在下次 recall 被回读，推动表层"对话姿态"准则细化为"短回复 150–300 字、纯文本、不编造历史、倾听式不给方法"。
- 之后数版本，对话姿态稳定符合准则。**这就是准则升级型进化。**

另一个片段：

- 深层"预期"节写下："若日记功能不加强隐私隔离，多成员家庭会出隐私事故。"
- 数版本后做日记功能时，agent 带着这条预警切入，把"日记只属于本人"做成了硬约束。**这是隐患预警型进化（时间维度的自我协作）。**

## 量化体感（主观）

- 接力新 session 的"找回状态"成本：从"重新读一堆代码 + 问人"降到"读 4 个文件"。
- 同类坑的复现率：明显下降（真因被记进中层，下次直接绕开）。
- 宗旨偏离的返工：基本消除（准则在表层每次都在场）。
- 总 token 消耗：下降——因为按需取更少更准，而不是塞更多。

## 关键经验

1. **表层宗旨必须逐字保留 + 仲裁优先级**。否则会被"更优雅的表述"悄悄改掉，初心在迭代中稀释。
2. **深层强制四节、强制"高于人类常规认知"**。否则退化成流水账，失去进化能力。
3. **中层写真因不写表象**。否则下次还会踩同一个坑。
4. **检查点是纪律，不是建议**。recall 跳一次就够漂移一次；consolidate 跳一次就少一次进化机会。
5. **密钥只写位置不写值**。记忆库会被 git 跟踪、可能公开，明文凭据是事故。

## 局限

- 文件检索在项目规模极大时不够锐利（本案例规模下完全够用）。解法：将来加索引/向量层，底层 Markdown 不变。
- 依赖 agent 遵守协议。模型越弱越容易跳过检查点——需要 hook/skill 强制，而非靠自觉。
- 深层质量取决于模型反思能力。弱模型会产出浅层反思，进化速度慢。