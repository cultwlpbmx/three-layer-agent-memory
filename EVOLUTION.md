# 自我进化机制（Evolution）

> 权重不变，行为也能进化——靠持久化的元认知闭环。
> 本文拆解 agent 如何在本范式下实现可追溯的自我改进。

## 进化的定义

- **权重级进化**：改模型参数（训练/微调）。成本高、不透明、不可逆。
- **范式级进化**：改 agent 自己的**信念与行为准则**。成本低、可读、可追溯。

本范式实现范式级进化。它不取代权重级进化，但在大多数应用场景里，范式级进化的**性价比远高于**微调——因为 agent 的大部分"犯错"不是因为它不会，而是因为它忘了自己曾经判定某条路不对。

## 闭环

```
   反思（深层追加） ──► 沉淀（落盘） ──► 回读（recall 第4步） ──► 修正准则（表层更新） ──► 新一轮反思
        ▲                                                                                    │
        └────────────────────────────────────────────────────────────────────────────────────┘
```

四个动作各自映射到一个协议检查点或文件：

| 动作 | 发生在 | 落点 |
|---|---|---|
| 反思 | consolidate 检查点 | `深层/AI深度思考.md` 追加一节 |
| 沉淀 | 写文件即完成 | 磁盘（git 可追溯） |
| 回读 | recall 检查点第 4 步 | 读深层末尾一节 |
| 修正准则 | consolidate 时若识别为结构性问题 | `表层/00-项目总览.md` 的"应用准则"区 |

## 三种进化产物

### 1. 准则升级（belief revision）

深层某个隐患反复出现（例如"又因为没先问'是否服务家庭关系'就加了功能，导致返工"），说明它不是偶发失误而是结构性问题。consolidate 时把它**升级为表层应用准则**：

> 任何新功能先问"是否服务 X" — 不服务就砍。

下一次 recall 第 1 步就会读到这条准则。agent 的行为边界被自己的历史经验收紧了。这就是进化。

### 2. 隐患预警（forward memory）

深层"预期"节写下的"若按当前轨迹，X 会爆"会被回读。当 agent 后来真的在 X 附近工作时，它带着这个预警切入，而不是从零开始。这等价于 agent 给未来的自己留了一张便条——**时间维度上的自我协作**。

### 3. 思想化石层（auditable cognition）

深层不删旧节。推翻旧结论时补"后续勘误"。于是 `AI深度思考.md` 是一条完整的判断演化链：

> 2026-06-10：认为方案 A 最优
> 2026-06-20：后续勘误，方案 A 在高并发下有 X 问题，改推方案 B
> 2026-07-05：方案 B 上线后验证，确认优于 A，原因 …

这条链让"我们为什么走到今天"可被审计、可被新人/新 agent 接力理解。这是组织级智能资产，不是个人笔记。

## 进化的防退化机制

进化能反向退化为"记流水账"。本范式用三条规则防退化：

1. **深层四节强制**：现状审视/优化方案/隐患/预期。强制"优化方案"节 = 强制想"结构性更优的路径"，不是复述做了什么。
2. **"高于人类常规认知范围"的明确要求**：挖隐式契约、结构性风险、二阶后果。这一句是质量标尺，写在模板里反复出现。
3. **宗旨仲裁**：准则升级不能违背表层逐字保留的宗旨。进化有边界，不会演化成与初心相反的东西。

## 何时该进化、何时该停

- **该进化**：同一类问题在深层出现 ≥2 次 → 升级为准则。
- **该停下追问**：单次异常不要急着改准则，先记进深层"观察哨"，等它复现。
- **永远不改**：表层宗旨/哲学逐字保留区。它们是进化的参照系，参照系本身不能被进化产物污染——要改宗旨只能由人显式给出新文本并重新逐字保留。

## 与多 agent 接力的关系

当多个 agent（或多人+多 agent）接力一个长项目，深层化石层成为**共享认知**：新接手的 agent 读深层就能继承前人的判断与预警，不必从零理解"为什么这样设计"。进化由此从"单 agent 自我改进"扩展为"群体认知积累"。这是本范式在团队/多 agent 场景下的最大杠杆。