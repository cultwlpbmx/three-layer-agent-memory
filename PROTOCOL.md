# 协议规范（Protocol）

> 这是本范式的"运行时"——agent 必须遵守的三个检查点。
> 与具体框架无关：Claude Code、Cursor、自研 agent 都按此执行。

## 设计原则

1. **按稳定性分层，而非按主题分类。** 越不容易变的东西越靠前读，越容易变的越靠后写、越频繁刷新。
2. **检索优先级：记忆库 → 代码/git → 问用户。** 能在库里解决的事，绝不把整段代码塞进上下文。
3. **只引用 `file:line`，不整段回贴**，除非用户明确要看。
4. **深层只追加，不删不改旧节。** 旧结论错了就在末尾补"后续勘误"，保留思想轨迹。
5. **宗旨/哲学若标注"逐字保留"，不得改写**，只能在其下补充说明。

---

## 检查点 1 — 开机 / 任务开始前（recall，必做）

目的：在消耗任何 token 做事之前，把项目的**初心、当前状态、上一次卡点、此前的隐患提醒**拉回工作记忆。

执行顺序：

1. 读 `表层/00-项目总览.md` → 找回目的、宗旨、哲学、固定偏好
2. 读 `表层/01-待完成任务.md` → 知道现在该做什么、卡在哪、优先级
3. 读 `中层/INDEX-任务流水.md` 最近 1–2 条 → 了解上一次做到哪、遇到什么困难
4. 读 `深层/AI深度思考.md` 末尾一节 → 看 AI 此前的隐患提醒与预期

> 不知道项目库路径时，先读库根 `INDEX.md` 列出所有项目库，再定位到目标项目。

**违反代价**：跳过 recall 直接干活 = 在长任务线上重新走一遍"丢失初心 → 偏离方向 → 返工"的老路。

---

## 检查点 2 — 阶段任务完成后（writeback，必做）

目的：把"这一段做了什么、卡在哪、真因是什么"落成可回溯的情景记忆。

执行：

1. 在 `中层/` 新建一篇任务记录，命名 `YYYY-MM-DD_<版本号或主题>_<简述>.md`，从 `_template/中层/_任务模板.md` 复制
2. 必填字段：
   - **时间戳**（开始→结束）
   - **任务清单**（`- [ ]` / `- [x]`）
   - **完成情况**（✅ 完成 / ⚠️ 部分 / ❌ 未完成，附关键 file:line、端点、版本号）
   - **遇到的困难**（写"真因"而非表象）
   - **遗留与下一步**（同步进表层 `01-待完成任务.md`）
3. 在 `中层/INDEX-任务流水.md` **顶部**插一行指针：`| 日期 | 文件名 | 一句话 |`

**粒度**：一个"阶段"= 一次可独立验证的产出（一个版本、一组反馈修复、一个模块上线）。不是每次对话都记，也不是攒一周才记。

---

## 检查点 3 — 每天结束 / 重大节点后（consolidate，必做）

目的：巩固慢变记忆 + 追加元认知。这是**自我进化**的发生点（见 `EVOLUTION.md`）。

执行：

1. 更新 `表层/01-待完成任务.md`：勾掉完成的、新增发现的、调整优先级（P0/P1/P2）
2. 更新 `表层/00-项目总览.md` 的"内容摘要""项目大纲"（仅在有实质进展时）
3. 在 `深层/AI深度思考.md` **追加**一段，带 `## YYYY-MM-DD <主题>` 分节，含四节：
   - **现状审视**：对当前产品/架构/进度的整体判断
   - **优化方案**：不是补丁，而是结构性更优的路径
   - **隐患**：还未爆但会爆的点——技术债、契约脆弱、数据/隐私/合规、增长瓶颈
   - **预期**：若按当前轨迹，短/中期会发生什么；应在何处设观察哨

> 深层是 AI 的智能体思考，应**高于人类常规认知范围**：挖隐式契约、结构性风险、二阶后果，而不是复述做了什么。

**深层铁律**：按日期分节追加，不拆文件、不删旧节。旧节结论被推翻时，在该节末尾补一行"> 后续勘误（YYYY-MM-DD）：…"。这样保留思想轨迹，便于追溯一个判断是怎么演化的。

---

## 冲突仲裁

- 同一事实，**表层**与外部记忆（如 Claude Code auto-memory）冲突时，以**本库表层**为准（更新更主动、更结构化），并在本库表层注明勘误。
- 表层宗旨/哲学（逐字保留区）与任何下游结论冲突时，**宗旨赢**。下游结论要么改，要么在深层记录"此处偏离宗旨，原因…"。

## 记忆稀释对抗（检索优先级）

```
本库 → 代码/git → 问用户
```

- 先查表层（快）→ 不够再查中层具体任务记录 → 还不够才查代码仓库
- 只引用 `file:line`，不整段回贴
- 问用户是最后手段，不是第一反应

## 与 agent 自动记忆的关系

本库放在磁盘，**不会自动注入上下文**。它依赖 agent 自身的轻量自动记忆（如 Claude Code 的 `MEMORY.md`）放一个**指针 + 使用说明**，在每次开机自动注入上下文，提醒 agent 来读本库。两套并行：

- 自动记忆：细粒度单点事实（一条一事）
- 本库：结构化三层长记忆

详细集成见 `INTEGRATION.md`。