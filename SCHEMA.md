# 存储模式（Schema）

> 磁盘上的物理布局与命名约定。纯 Markdown，零基础设施，git 友好。

## 顶层布局

一个"记忆库根"可容纳多个项目库：

```
项目记忆库/                       # 库根
├── INDEX.md                      # 总索引：所有项目库一行指针
├── README.md                     # 本库使用说明（库级，非项目级）
├── _template/                    # 新建项目库的模板
└── <项目名>/                     # 一个项目库
    ├── 表层/
    ├── 中层/
    └── 深层/
```

`INDEX.md` 格式：

```markdown
| 项目 | 路径 | 一句话定位 | 最后更新 |
|------|------|-----------|----------|
| <项目名> | `<项目名>/` | <一句话> | YYYY-MM-DD |
```

## 项目库内部

```
<项目名>/
├── 表层/
│   ├── 00-项目总览.md            # 慢变：名称/目的/宗旨/哲学/需求/偏好/服务器指引/内容摘要/项目大纲
│   └── 01-待完成任务.md          # 高频：P0/P1/P2 + 已完成近 10 条
├── 中层/
│   ├── INDEX-任务流水.md         # 时间线索引，新条目置顶
│   └── YYYY-MM-DD_<版本或主题>_<简述>.md   # 每阶段一篇
└── 深层/
    └── AI深度思考.md              # 按 ## YYYY-MM-DD 分节追加
```

## 字段约定

### 表层 / 00-项目总览.md

| 小节 | 稳定性 | 说明 |
|---|---|---|
| 项目名称 | 极稳 | |
| 一句话定位 | 极稳 | 是什么/给谁/解决什么 |
| 核心目的 | 极稳 | 1–3 句 |
| 核心宗旨 | 极稳 | **逐字保留**，所有迭代以此为准绳 |
| 哲学思想 | 极稳 | **逐字保留** |
| 用户需求 | 稳 | 真实需求列表 |
| 固定偏好 | 稳 | 技术栈/工作风格/沟通偏好/不可动偏好 |
| 服务器与密钥 | 稳 | **只写"在哪里取"，不明文回贴密钥** |
| 内容摘要 | 慢变 | 最近大节点，每有实质进展更新 |
| 项目大纲 | 慢变 | 模块/版本脉络，便于定位 |

> ⚠️ 密钥安全：表层只写密钥的**位置指引**（如"`/root/x/.env` 含 ALIYUN_API_KEY"），**永远不**在记忆库明文存放可执行凭据。记忆库会被 git 跟踪、可能公开。

> 🔒 **强制**：推送前跑 `scripts/secret-scan.sh`（或启用 `.github/workflows/secret-scan.yml` CI）。它 grep 常见密钥模式（ghp_/gho_/AKIA/私钥头/带密码的 DB URI 等），命中即阻断。这是"忘了只写位置"的最后一道网。

### 表层 / 01-待完成任务.md

- 用 `- [ ]` / `- [x]` 标记
- 优先级 `P0`（当前主线）/ `P1`（近期）/ `P2`（待定）
- "已完成"只留最近 10 条，更旧的进中层归档

### 中层 / 任务记录

文件名：`YYYY-MM-DD_<版本号或主题>_<简述>.md`
例：`2026-06-25_V5.4.14_测试反馈修复.md`

字段见 `PROTOCOL.md` 检查点 2：时间戳、任务清单、完成情况、遇到的困难、关键产出、遗留与下一步、关联。

### 中层 / INDEX-任务流水.md

```markdown
| 日期 | 任务记录 | 一句话 |
|------|----------|--------|
| 2026-06-25 | `2026-06-25_V5.4.14_测试反馈修复.md` | 十项测试反馈修复全完成并部署 |
```

新条目**置顶**插入。

### 深层 / AI深度思考.md

- 按 `## YYYY-MM-DD <主题>` 分节**追加**
- 不拆文件、不删旧节
- 四子节：现状审视 / 优化方案 / 隐患 / 预期
- 旧结论被推翻 → 在旧节末尾补 `> 后续勘误（YYYY-MM-DD）：…`

## 命名约定总表

| 对象 | 约定 |
|---|---|
| 项目库目录 | 项目名（中英文均可，建议与代码仓库名对齐） |
| 中层任务文件 | `YYYY-MM-DD_<版本或主题>_<简述>.md` |
| 深层分节 | `## YYYY-MM-DD <主题>` |
| 中层模板 | `_任务模板.md`（留作复制，不删除） |
| 表层文件 | `00-项目总览.md`、`01-待完成任务.md`（数字前缀保证排序） |

## 本地化（Localization）

> **目录名可本地化，三层语义不变。** 范式不绑定中文。
> `_template/` 是中文布局（canonical），`_template-en/` 是英文布局；复制其一即可。
> 参考实现 `examples/memory_adapter.py` 自动探测两种布局。

中英文件名映射：

| 层 | 中文（canonical） | 英文 |
|---|---|---|
| 表层目录 | `表层/` | `Surface/` |
| 中层目录 | `中层/` | `Middle/` |
| 深层目录 | `深层/` | `Deep/` |
| 表层总览 | `00-项目总览.md` | `00-overview.md` |
| 表层待办 | `01-待完成任务.md` | `01-todo.md` |
| 中层索引 | `INDEX-任务流水.md` | `INDEX-task-log.md` |
| 中层模板 | `_任务模板.md` | `_task-template.md` |
| 深层反思 | `AI深度思考.md` | `AI-deep-reflection.md` |

混用不建议（一个项目库内统一用一种语言布局），但跨项目库可以各自选用。

## 为什么用文件系统而不用数据库

1. **人类可读**：用户能直接打开看、直接编辑修正。
2. **git 可 diff**：记忆的演化本身是有价值的数据，可追溯。
3. **零基础设施**：不需要起服务、不需要向量库、不需要 embedding。
4. **跨 agent 可移植**：任何能读文件的 agent 都能用，不被某个框架锁定。
5. **抗上下文污染**：按需读取，不是全量塞进上下文。

如果将来规模大到文件检索不够用，再加索引/向量层即可——但**底层存储仍是这些 Markdown 文件**，索引只是派生物。