# 存储模式（Schema）

> 磁盘上的物理布局与命名约定。纯 Markdown，零基础设施，git 友好。

## 顶层布局

一个"记忆库根"可容纳多个项目库：

```
项目记忆库/                       # 库根
├── INDEX.md                      # 总索引：所有项目库一行指针
├── README.md                     # 本库使用说明（库级，非项目级）
├── _template/                    # 新建项目库的模板
└── <项目名>/                     # 一个项目库
    ├── 表层/
    ├── 中层/
    └── 深层/
```

`INDEX.md` 格式：

```markdown
| 项目 | 路径 | 一句话定位 | 最后更新 |
|------|------|-----------|----------|
| <项目名> | `<项目名>/` | <一句话> | YYYY-MM-DD |
```

## 项目库内部

```
<项目名>/
├── 表层/
│   ├── 00-项目总览.md            # 慢变：名称/目的/宗旨/哲学/需求/偏好/服务器指引/内容摘要/项目大纲
│   ├── 01-待完成任务.md          # 高频：P0/P1/P2 + 已完成近 10 条
│   └── 02-未知与开放问题.md       # 中频：认知缺口 + 开放问题（可选，推荐）
├── 中层/
│   ├── INDEX-任务流水.md         # 时间线索引，新条目置顶
│   ├── YYYY-MM-DD_<版本或主题>_<简述>.md   # 每阶段一篇（含 tags 行）
│   ├── _任务模板.md               # 留作复制
│   └── archive/                  # 归档区（超阈值的历史记录）
└── 深层/
    └── AI深度思考.md              # 按 ## YYYY-MM-DD 分节追加
```

> 全局深层库（跨项目）在用户级目录 `~/.agent-memory/global-deep/`，不在项目库内。见下方"全局深层库"节。

## 字段约定

### 表层 / 00-项目总览.md

| 小节 | 稳定性 | 说明 |
|---|---|---|
| 项目名称 | 极稳 | |
| 一句话定位 | 极稳 | 是什么/给谁/解决什么 |
| 核心目的 | 极稳 | 1–3 句 |
| 核心宗旨 | 极稳 | **逐字保留**，所有迭代以此为准绳 |
| 哲学思想 | 极稳 | **逐字保留** |
| 用户需求 | 稳 | 真实需求列表 |
| 固定偏好 | 稳 | 技术栈/工作风格/沟通偏好/不可动偏好 |
| 服务器与密钥 | 稳 | **只写"在哪里取"，不明文回贴密钥** |
| 内容摘要 | 慢变 | 最近大节点，每有实质进展更新 |
| 项目大纲 | 慢变 | 模块/版本脉络，便于定位 |

> ⚠️ 密钥安全：表层只写密钥的**位置指引**（如"`/root/x/.env` 含 ALIYUN_API_KEY"），**永远不**在记忆库明文存放可执行凭据。记忆库会被 git 跟踪、可能公开。

> 🔒 **强制**：推送前跑 `scripts/secret-scan.sh`（或启用 `.github/workflows/secret-scan.yml` CI）。它 grep 常见密钥模式（ghp_/gho_/AKIA/私钥头/带密码的 DB URI 等），命中即阻断。这是"忘了只写位置"的最后一道网。

### 表层 / 01-待完成任务.md

- 用 `- [ ]` / `- [x]` 标记
- 优先级 `P0`（当前主线）/ `P1`（近期）/ `P2`（待定）
- "已完成"只留最近 10 条，更旧的进中层归档

### 表层 / 02-未知与开放问题.md（可选，推荐）

| 小节 | 说明 |
|---|---|
| 认知缺口 | "我知道 A 但不知道 B"格式的知识边界声明 |
| 开放问题 | 尚未有答案、但值得持续关注的问题 |

> 这不是任务清单（做什么），是知识地图的空白区（还不懂什么）。让 agent 知道自己的认知边界，避免"以为懂了其实不懂"的循环。

### 中层 / 任务记录

文件名：`YYYY-MM-DD_<版本号或主题>_<简述>.md`
例：`2026-06-25_V5.4.14_测试反馈修复.md`

字段见 `PROTOCOL.md` 检查点 2：时间戳、版本/分支、**agent**、任务清单、完成情况、遇到的困难、关键产出、遗留与下一步、关联。

> **agent 签名**（v0.5 起）：每篇任务记录元数据区必含 `- **agent**: <名称>` 行，标注本篇是哪个 agent 写的（如 `claude-code` / `codex` / `cursor`）。这是跨 agent 协作可追溯性的基础——当多个 agent 接力同一项目，签名让“谁写了什么”一目了然，而不是混在一起无法归因。`validate()` 会对缺签名的近期记录报 WARN。

### 中层 / INDEX-任务流水.md

```markdown
| 日期 | 任务记录 | 一句话 |
|------|----------|--------|
| 2026-06-25 | `2026-06-25_V5.4.14_测试反馈修复.md` | 十项测试反馈修复全完成并部署 |
```

新条目**置顶**插入。

### 深层 / AI深度思考.md

- 按 `## YYYY-MM-DD <主题>` 分节**追加**
- 每节头部含 `> agent: <名称>` 签名行（v0.5 起），标注本节反思是哪个 agent 写的
- 不拆文件、不删旧节
- 四子节：现状审视 / 优化方案 / 隐患 / 预期
- 旧结论被推翻 → 在旧节末尾补 `> 后续勘误（YYYY-MM-DD）：…`

## 中层归档（防膨胀）

中层任务记录只追加不删，但用久了会积累大量文件。当数量超过阈值（默认 20 篇），启动归档：

### 归档动作

1. 将最早的篇目（超出阈值的部分）移入 `中层/archive/`
2. 原文不修改、不压缩——只是移动位置
3. 在 `INDEX-任务流水.md` 中，归档条目的行替换为摘要行格式：

```markdown
| 日期 | `archive/<文件名>` | <一句话> · 关键真因：<一句话教训> |
```

### 归档摘要行要求

摘要行是"看一眼决定要不要去 archive 读原文"的过滤器，必须包含：
- 原始一句话描述
- `· 关键真因：` + 一句话教训（不是复述，是"如果早知道这条能省什么事"）

### 触发条件

- 阈值默认 20 篇，可在表层总览的"固定偏好"中调整
- 归档在 consolidate 时执行（检查点 3）
- 首次归档不急——20 篇以下不归档，保证近期记忆全量可见

### 归档铁律

- **只移动，不修改**：归档原文不改动一字，只是从 `中层/` 移到 `中层/archive/`
- **INDEX 保留摘要行**：归档不是删除索引，是把索引行替换为更短的摘要行
- **可逆**：需要时随时从 archive 移回（git 历史也在）

```
<项目名>/中层/
├── INDEX-任务流水.md         # 含近期待办 + 归档摘要行
├── YYYY-MM-DD_*.md            # 近期任务记录（全量可见）
├── _任务模板.md
└── archive/                   # 归档区（超阈值的历史记录）
    ├── 2026-06-27_推送_上线.md
    └── 2026-06-27_范式提取_建立.md
```

## 中层标签（联想回溯）

人类记忆是**联想触发**的：遇到一个类似场景，相关记忆自动浮现。当前范式的 recall 是"开机全量扫描"，在中层条目少时无碍，但条目多了以后 agent 不可能每次全读。

标签是零依赖的联想检索：中层每篇任务记录在元数据区加一行 `tags:`，adapter 的 recall 增加 `--tag` 参数用 grep 过滤。

### 标签格式

在任务记录的元数据区（时间戳附近）加一行：

```markdown
- **时间戳**：YYYY-MM-DD（开始）→（结束）
- **版本/分支**：V5.4.14
- **tags**：#鉴权 #网络 #GitHub
- **入口**：...
```

### 标签约定

- 用 `#` 前缀，空格分隔，全小写推荐
- 标签是**自由开放**的——不强制封闭列表，随用随加，自然收敛
- SCHEMA 推荐一组常见标签供参考（非强制）：`#部署` `#鉴权` `#网络` `#测试` `#修复` `#架构` `#安全` `#文档` `#重构` `#部署` `#数据库` `#前端` `#后端`
- 英文布局用英文标签：`#deploy #auth #network #testing #fix #architecture #security #docs #refactor #database #frontend #backend`
- 一个任务记录可以有多个标签（跨主题的任务尤其需要）

### recall 的标签过滤

adapter `recall --tag 鉴权` 时，只返回 INDEX 中含 `#鉴权` 标签的任务记录摘要。这让 agent 在遇到鉴权相关任务时，能快速回溯所有相关历史，而不是全量扫描。

## 全局深层库（跨项目记忆）

人类不是每个项目一个大脑——在项目 A 里踩过的坑，在项目 B 里不会再踩。当前范式默认项目隔离，但跨项目记忆是"改变 AI 记忆方式"的核心差异点。

### 布局

```
~/.agent-memory/                       # 全局记忆根（用户级，不在任何项目库内）
└── global-deep/
    └── global-reflection.md           # 跨项目经验法则，按 ## YYYY-MM-DD 追加
```

### 规则

- 全局深层与项目深层格式一致：`## YYYY-MM-DD <主题>` 分节追加，不删旧节
- 全局深层只接受**跨项目经验法则**——即"来自 N≥2 次实践的跨项目通则"。单项目经验留在项目深层
- 新项目 recall 时，若全局深层存在，先读其末尾一节——agent 上手就带着所有项目的经验
- 项目 consolidate 时，若提炼出的法则具有跨项目通用性，同时写入项目深层和全局深层
- 全局深层是**用户级**的（`~/.agent-memory/`），不进任何项目仓库，不推 GitHub

### 与项目深层的关系

| 层 | 范围 | 内容 |
|---|---|---|
| 项目深层 | 单项目 | 本项目的架构判断、隐患、进化 |
| 全局深层 | 跨项目 | 跨项目通用的经验法则（如"鉴权发现优先级"） |

项目深层是主库，全局深层是汇总。冲突时以项目深层为准（更具体、更近）。

## 表层 / 02-未知与开放问题.md

人类最强的认知能力之一是**知道自己的知识边界**——"我知道部署需要鉴权，但我不知道怎么配 SSO"。这让人类在遇到相关问题时知道该去查什么，而不是以为自己已经懂了。

### 内容

记录 agent 明确知道的认知缺口——不是"要做的事"（那是待完成任务），而是"还不懂的事"（知识地图的空白区）：

```markdown
## 认知缺口
- **<领域>**：我知道 X 存在，但不理解 Y。若遇到 Y 相关问题，需先查 <指引>。
- **鉴权**：我知道 GitHub 需要令牌，但不了解 GitHub Apps 的权限模型差异。若涉及组织级自动化，需查 GitHub App 文档。

## 开放问题
- <尚未有答案、但值得持续关注的问题>
```

### 更新时机

- recall 时：如果当前任务触及某个已知缺口，优先补查该缺口的答案
- consolidate 时：如果本阶段发现了新的"以为懂了其实不懂"的事，记进认知缺口
- 当某个缺口被填上（搞懂了），移到"已解决"并在深层记一条

### 字段约定

| 小节 | 说明 |
|---|---|
| 认知缺口 | "我知道 A 但不知道 B"格式的知识边界声明 |
| 开放问题 | 尚未有答案、但值得持续关注的问题 |

## 命名约定总表

| 对象 | 约定 |
|---|---|
| 项目库目录 | 项目名（中英文均可，建议与代码仓库名对齐） |
| 中层任务文件 | `YYYY-MM-DD_<版本或主题>_<简述>.md` |
| 深层分节 | `## YYYY-MM-DD <主题>` |
| 中层模板 | `_任务模板.md`（留作复制，不删除） |
| 中层归档 | `中层/archive/`（超阈值的历史记录） |
| 表层文件 | `00-项目总览.md`、`01-待完成任务.md`、`02-未知与开放问题.md`（数字前缀保证排序） |
| 全局深层 | `~/.agent-memory/global-deep/global-reflection.md`（跨项目经验法则） |

## 本地化（Localization）

> **目录名可本地化，三层语义不变。** 范式不绑定中文。
> `_template/` 是中文布局（canonical），`_template-en/` 是英文布局；复制其一即可。
> 参考实现 `examples/memory_adapter.py` 自动探测两种布局。

中英文件名映射：

| 层 | 中文（canonical） | 英文 |
|---|---|---|
| 表层目录 | `表层/` | `Surface/` |
| 中层目录 | `中层/` | `Middle/` |
| 深层目录 | `深层/` | `Deep/` |
| 表层总览 | `00-项目总览.md` | `00-overview.md` |
| 表层待办 | `01-待完成任务.md` | `01-todo.md` |
| 表层未知 | `02-未知与开放问题.md` | `02-unknowns.md` |
| 中层索引 | `INDEX-任务流水.md` | `INDEX-task-log.md` |
| 中层模板 | `_任务模板.md` | `_task-template.md` |
| 中层归档 | `中层/archive/` | `Middle/archive/` |
| 深层反思 | `AI深度思考.md` | `AI-deep-reflection.md` |
| 全局深层 | `~/.agent-memory/global-deep/global-reflection.md` | 同（不分语言） |

混用不建议（一个项目库内统一用一种语言布局），但跨项目库可以各自选用。

## 为什么用文件系统而不用数据库

1. **人类可读**：用户能直接打开看、直接编辑修正。
2. **git 可 diff**：记忆的演化本身是有价值的数据，可追溯。
3. **零基础设施**：不需要起服务、不需要向量库、不需要 embedding。
4. **跨 agent 可移植**：任何能读文件的 agent 都能用，不被某个框架锁定。
5. **抗上下文污染**：按需读取，不是全量塞进上下文。

如果将来规模大到文件检索不够用，再加索引/向量层即可——但**底层存储仍是这些 Markdown 文件**，索引只是派生物。